version: "3.7"

services:
  app:
    image: vaultwarden/server:1.30.3
    restart: on-failure
    ports:
      - "${APP_PORT}:80"
    environment:
      - WEBSOCKET_ENABLED=true
      - SIGNUPS_ALLOWED=true
      - ADMIN_TOKEN=umbrel123
    volumes:
      - "${APP_DATA_DIR}/data:/data"
